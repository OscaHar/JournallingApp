extends layout

block content
    div.header--login.header 
        p.header--BigText Journaling
        p.header--SubText APP

    div.text--login
        p.text-login.text--Biglogin LET'S GET STARTED
        p.text-login.text--SubLogin ...and join the hype!
    p.text-login.text--Or or
      
    button(class="btn--login").btn
        span.btnTextCenter LOGIN
  
    button(class="btn--register").btn
        span.btnTextCenter REGISTER
